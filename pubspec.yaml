name: demo
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  shared_preferences: ^2.0.7
  sqflite: ^2.0.0+3  # Add the sqflite package
  path_provider: ^2.0.2  # Add this line for path_provider
  fl_chart: ^0.67.0
  intl: ^0.19.0
  leak_tracker: ^10.0.0
  leak_tracker_flutter_testing: ^2.0.1
  leak_tracker_testing: ^2.0.1
  material_color_utilities: ^0.8.0
  meta: ^1.11.0
  test_api: ^0.6.1
  vm_service: ^13.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/image/background.jpg
